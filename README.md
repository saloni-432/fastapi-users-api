# FastAPI User CRUD API

A REST API built with FastAPI, PostgreSQL and Docker.

## Features
- User CRUD operations
- PostgreSQL database
- Dockerized setup
- Pydantic validation
- SQLAlchemy ORM

## Run locally

docker compose up --build

Open:
http://localhost:8000/docs